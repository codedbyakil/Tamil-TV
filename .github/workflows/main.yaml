name: Auto-Update Tamil M3U

permissions:
  contents: write

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Generate tamil.m3u
        run: |
          echo "📥 Fetching Ziotv.json..."
          JSON_URL="https://playify.pages.dev/Ziotv.json"
          JSON=$(curl -sf \
            -H "User-Agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/119.0.0.0 Safari/537.36" \
            -H "Accept: application/json" \
            -H "Referer: https://playify.pages.dev/" \
            "$JSON_URL") || { echo "❌ Failed to fetch JSON"; exit 1; }

          if [ -z "$JSON" ] || [ "${JSON:0:1}" = "<" ]; then
            echo "❌ Response is empty or HTML"
            exit 1
          fi

          echo "#EXTM3U" > tamil.m3u

          # ✅ Exact channel names (case-insensitive match)
          # Preserve spaces, "HD", "Tamil", etc. — no cleaning
          echo "$JSON" | jq -r '
            # Normalize structure
            def channels:
              if type == "array" then .
              elif type == "object" then
                if has("channels") and (.channels | type == "array") then .channels
                elif has("data") and (.data | type == "array") then .data
                elif has("list") and (.list | type == "array") then .list
                elif (.[] | type == "object") then [.[]]
                else []
                end
              else []
              end;

            # Your exact list (lowercase for safe compare)
            [
              "sun tv hd",
              "sun music hd",
              "sun life",
              "adithya tv",
              "ktv hd",
              "star vijay hd",
              "vijay super",
              "colors tamil",
              "jaya tv hd",
              "jaya plus",
              "j movies",
              "polimer tv",
              "vasanth tv",
              "kalaignar tv",
              "raj tv",
              "disney channel",
              "disney junior",
              "hungama",
              "super hungama",
              "sonic tamil",
              "nick tamil",
              "nickelodeon jr.",
              "pogo tamil",
              "cartoon network tamil",
              "cn hd+ tamil",
              "discovery kids tamil",
              "d tamil",
              "history tv18 hd tamil"
            ] as $allowlist |

            channels |
            map(select(type == "object")) |
            map(
              (.name // .title // "") as $name |
              select(
                ($name | ascii_downcase) as $n |
                ($allowlist | index($n)) != null and
                (.link // .url // "") != ""
              )
            ) |
            # Deduplicate by exact name (keep first)
            group_by(.name // .title // "") |
            map(.[0]) |
            .[] |

            (.name // .title // "Unknown") as $name |
            (.tvg_id // .tvgId // .id // "") as $tvg_id |
            (.tvg_logo // .logo // .icon // "") as $tvg_logo |
            (.drmScheme // .drm_scheme // .drm // "") as $drmScheme |
            (.drmLicense // .license // "") as $drmLicense |
            (.cookie // .cookies // "") as $cookie |
            (.link // .url // .stream_url // .hls // .mpd // "") as $link |

            "#EXTINF:-1 " +
              (if ($tvg_id | length > 0) then "tvg-id=\"\($tvg_id)\" " else "" end) +
              "group-title=\"Tamil\" " +
              (if ($tvg_logo | length > 0) then "tvg-logo=\"\($tvg_logo)\" " else "" end) +
              ",\($name | gsub(","; "\\,"))" |
            . as $extinf |

            (if ($drmScheme == "clearkey") and ($drmLicense | test("^[a-zA-Z0-9]+:[a-zA-Z0-9]+")) then
              "#KODIPROP:inputstream.adaptive.license_type=clearkey\n#KODIPROP:inputstream.adaptive.license_key=" + 
              ($drmLicense | split(":")[0:2] | join(":"))
            else "" end) as $drm |

            "#EXTVLCOPT:http-user-agent=plaYtv/7.1.5%20(Linux%3BAndroid%2015)%20ExoPlayerLib/2.11.6%20YGX/69.69.69.69" as $ua |

            (if ($cookie | length > 0) then
              "#EXTHTTP:{\"cookie\":\"" + ($cookie | gsub("\""; "\\\"")) + "\"}"
            else "" end) as $cookie_line |

            [
              $extinf,
              ($drm | select(. != "")),
              $ua,
              ($cookie_line | select(. != "")),
              $link
            ] | join("\n")
          ' >> tamil.m3u

          # Validation
          LINE_COUNT=$(wc -l < tamil.m3u)
          CHANNEL_COUNT=$(grep -c "^#EXTINF" tamil.m3u)
          if [ "$LINE_COUNT" -le 1 ]; then
            echo "❌ tamil.m3u has only #EXTM3U"
            echo "🔍 Available channel names in JSON (first 30):"
            echo "$JSON" | jq -r 'channels | .[0:30][] | .name // .title // "–"' 2>/dev/null
            exit 1
          fi

          echo "✅ tamil.m3u created: $CHANNEL_COUNT channels"
          echo "📋 Matched channels:"
          grep "^#EXTINF" tamil.m3u | sed 's/.*,//' | sort

      - name: Commit & Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tamil.m3u

          if git diff --cached --quiet; then
            echo "➡️ No changes — skipping commit"
            exit 0
          fi

          git commit -m "✅ Auto-update Tamil M3U: $CHANNEL_COUNT channels | $(date -u '+%Y-%m-%d %H:%M')"
          git push origin main
